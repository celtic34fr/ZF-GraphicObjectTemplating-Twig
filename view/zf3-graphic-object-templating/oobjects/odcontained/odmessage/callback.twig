    {% if objet.event is not empty %}
    callback: function ($this, type, e) {
        let {{ objet.id }} = new {{ objet.object }}($('#{{ objet.id }}'));
        invokeAjax({{ objet.id }}.getData(type), '{{ objet.id }}', 'click', e);
        $this.hide();
    },
    {% endif %}